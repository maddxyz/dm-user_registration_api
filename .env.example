# Database
POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password
POSTGRES_DB=registration_db
DATABASE_URL=postgresql://app_user:app_password@db:5432/registration_db

# Email
# Options: "console" (prints to stdout) | "http" (calls third-party HTTP API)
EMAIL_SERVICE=console
